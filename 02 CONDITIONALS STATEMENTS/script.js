let age = 45;

if (age > 18) {
    console.log("You can drive")
}
else{
    console.log("You can't drive")
}

// IF you want to conpare value with types you can use === and if you want to compair only value without types you can use only ==

// all operators

// Ternary operators
let p = 5;
let q = 4;
let d = (p > q)? (p - q):(p + q);
console.log(d);